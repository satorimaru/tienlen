# Upstash Redis (required for multiplayer across devices / Vercel)
# Create a free database at https://console.upstash.com
# Or connect via Vercel Marketplace → Upstash

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Local dev works without these (in-memory rooms, single Node process only).
